---
import { getCollection } from "astro:content";
import Base from "../../../layouts/Base.astro";
import { withBase } from "../../../i18n";

const posts = (await getCollection("blog", ({ data }) => data.lang === "ja")).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
const fmt = new Intl.DateTimeFormat("ja", { dateStyle: "medium" });
const hreflangUrls = {
  zh: withBase("/blog/"),
  en: withBase("/en/blog/"),
  ja: withBase("/ja/blog/"),
  ko: withBase("/ko/blog/"),
};
---

<Base locale="ja" title="ブログ · Pier" description="Pier のバージョン更新" hreflangUrls={hreflangUrls}>
  <section class="mx-auto max-w-3xl px-5 py-16">
    <p class="section-label"><em>Blog</em>バージョン更新</p>
    <h1 class="display mt-4 text-3xl sm:text-4xl">Pier ブログ</h1>
    <p class="mt-3 text-[15px] text-muted">いま掲載しているのはバージョン更新です。</p>

    <ul class="mt-10">
      {posts.map((post) => (
        <li>
          <a
            href={withBase(`/ja/blog/${post.id.split("/")[1]}/`)}
            class="group block border-line border-b py-6"
          >
            <time class="font-mono text-xs text-faint">{fmt.format(post.data.pubDate)}</time>
            <h2 class="mt-1.5 text-xl font-semibold transition-colors group-hover:text-brand-ink">
              {post.data.title}
            </h2>
            <p class="mt-2 text-sm leading-relaxed text-muted">{post.data.description}</p>
          </a>
        </li>
      ))}
    </ul>
  </section>
</Base>
